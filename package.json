{
  "name": "bookshelf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared reading tracker with automatic series-release watching",
  "engines": { "node": ">=22" },
  "scripts": {
    "books": "node src/cli.js",
    "check": "node src/run-check.js",
    "serve": "node --run-serve 2>/dev/null || npx --yes serve docs"
  }
}
